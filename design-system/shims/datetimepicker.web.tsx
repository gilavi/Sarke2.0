// Web stub for @react-native-community/datetimepicker (native OS picker, no web
// build). DateTimeField renders its own trigger field/chips; only the spinner
// modal is native. Stubbing lets the field render in the showcase. Mirrors the
// metro WEB_SHIMS pattern.
export default function DateTimePicker(): null {
  return null;
}
