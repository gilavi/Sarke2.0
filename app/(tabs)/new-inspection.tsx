// Stub screen — never actually rendered.
// The "new-inspection" tab slot uses a custom FAB button (tabBarButton)
// that navigates to the /new-inspection modal instead of this screen.
import { View } from 'react-native';

export default function NewInspectionStub() {
  return <View />;
}
