// Drafts screen — every in-progress record (all types), reached from the More
// tab. Completed records never mix with drafts (they live in the per-type
// widgets/lists on Home, History, and the project screen). Route file stays
// orchestration-only; the body lives in features/drafts/.
export { default } from '../features/drafts/DraftsScreen';
