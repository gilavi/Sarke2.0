node_modules/
.expo/
.expo-shared/
dist/
*.log
.DS_Store
.env
.env.local
.env.*.local
.claude/

# Native build output from `expo prebuild` / `expo run:ios|android`.
# This is a managed Expo project — native dirs regenerate on demand
# and shouldn't be tracked.
ios/
android/


# Workflow file — has to be added via GitHub web UI because the OAuth
# token gh CLI uses lacks the `workflow` scope. Run
# `gh auth refresh -s workflow` to enable pushing it from the CLI.
.github/workflows/deploy-web.yml
