# Edge function endpoint. Defaults to the production project; override for a
# local `supabase functions serve` or the staging project.
VITE_CMS_FN_URL=https://seskuthiopywrgntsgfw.supabase.co/functions/v1/cms-texts

# Set to 1 to run the UI against in-memory mock data (no backend). Dev only.
# VITE_CMS_MOCK=1
